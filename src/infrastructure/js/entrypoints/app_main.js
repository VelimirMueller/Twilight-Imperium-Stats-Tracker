/* eslint-disable object-curly-newline */ 
import { createApp } from 'vue';
import MainWrapper from '../../../../components/app/main/MainWrapper.vue'

createApp({}).component('MainWrapper', MainWrapper).mount('#app')